require 'spec_helper'

describe Weather::Icons::Sass do
  it 'has a version number' do
    expect(Weather::Icons::Sass::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
